import { TextField } from '@mui/material';

const SearchField = () => {
  return (
    <>
      <TextField
        placeholder="Search"
        variant="outlined"
      />
    </>
  );
};

export default SearchField;
